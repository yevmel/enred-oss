{
  "name": "enred-oss",
  "type": "module",
  "private": true,
    "exports": {
        ".": "./src/typescript/mount.ts",
        "./*": "./src/typescript/*"
    },
    "scripts": {
        "dev":   "mkdir -p dist && cp src/index.html dist/ && bun build src/typescript/index.ts --outdir dist --watch",
        "build": "mkdir -p dist && cp src/index.html dist/ && bun build src/typescript/index.ts --outdir dist --minify && cp src/index.html dist/",
        "serve": "bunx serve dist -l 3000 --single",
        "test":  "bun test"
    },
    "devDependencies": {
        "@types/bun": "latest"
      },
    "dependencies": {
        "lit-html": "^3.2.1"
    },
    "peerDependencies": {
    "typescript": "^5"
  }
}
